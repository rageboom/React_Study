var should = require('chai').should(),
    history = require('../index');

describe('#History.write', function() {
  it('Should create a history record', function() 
  {

    history.Write('video','Finish Editing video','done','','http://localhost:1337/history/create',function(err,data) 
    {
      if(err)
        console.log(err);
      else
          console.log('The data: '+data);
    });

  });
});